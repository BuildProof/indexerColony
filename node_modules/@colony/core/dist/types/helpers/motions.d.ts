import { BigNumber, type BigNumberish, type BytesLike } from 'ethers';
import { type CommonColony, type CommonNetwork, type CommonVotingReputation } from './types.js';
import { type ReputationClient } from './reputation.js';
/**
 * Gets the necessary proofs for motion creation
 *
 * This gets the reputation and domain proofs for motion creation
 *
 * @param contract - Any VotingReputation extension contract
 * @param domainId - Domain id the motion will be created in
 * @param altTarget - Target address for the motion (0x0 if Colony contract)
 * @param action - The encoded action the motion will execute when finalized
 *
 * @returns The necessary reputation and domain proofs to create a motion
 */
export declare function getCreateMotionProofs(network: CommonNetwork, colony: CommonColony, reputation: ReputationClient, votingReputation: CommonVotingReputation, domainId: BigNumberish, altTarget: string, action: BytesLike): Promise<{
    actionCid: BigNumber;
    key: string;
    value: string;
    branchMask: string;
    siblings: string[];
}>;
