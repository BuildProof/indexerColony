import { ClientType } from '../constants.js';
import { TokenLocking__factory as TokenLockingFactory, } from '../contracts/index.js';
const getTokenLockingClient = (address, signerOrProvider) => {
    const tokenLockingClient = TokenLockingFactory.connect(address, signerOrProvider);
    tokenLockingClient.clientType = ClientType.TokenLockingClient;
    return tokenLockingClient;
};
export default getTokenLockingClient;
