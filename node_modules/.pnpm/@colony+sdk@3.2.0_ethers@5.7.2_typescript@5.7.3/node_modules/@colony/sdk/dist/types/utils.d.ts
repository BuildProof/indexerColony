import type { Interface } from '@ethersproject/abi';
import { type ContractReceipt } from 'ethers';
import { type ParsedLogTransactionReceipt } from './types.js';
/** Extract event args from a contract receipt */
export declare const extractEvent: <T>(eventName: string, receipt: ContractReceipt | ParsedLogTransactionReceipt) => T | undefined;
/** Manually extract an event using the interface (e.g. if emitting contract is a different one than the calling contract) */
export declare const extractCustomEvent: <T>(eventName: string, receipt: ContractReceipt | ParsedLogTransactionReceipt, iface: Interface) => T | undefined;
