import ts from "typescript";
import { Metadata } from "../metadata/Metadata";
import { MetadataCollection } from "./MetadataCollection";
export declare namespace MetadataFactory {
    interface IOptions {
        resolve: boolean;
        constant: boolean;
        validate?: (meta: Metadata) => void;
    }
    const analyze: (checker: ts.TypeChecker) => (options: IOptions) => (collection: MetadataCollection) => (type: ts.Type | null) => Metadata;
    /**
     * @deprecated Use `analyze` function instead
     */
    const generate: (checker: ts.TypeChecker, collection: MetadataCollection, type: ts.Type, options: IOptions) => Metadata;
}
